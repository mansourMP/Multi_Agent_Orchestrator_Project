Now I have the full picture. Here’s everything broken down clearly.

What OpenClaw actually is vs what Empyralist is

┌─────────────────────────────────────────────────────────────┐
│                        OPENCLAW                             │
│                                                             │
│  Personal AI assistant that runs on YOUR machine           │
│  You talk to it via WhatsApp/Telegram/Slack                 │
│  It executes tasks: emails, files, browser, code           │
│  No visual canvas. No workflow builder. Just chat.         │
│  Single user. Single agent (with sub-agents internally)    │
│  247,000 GitHub stars. Jensen Huang called it              │
│  "probably the most important software release ever"       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                       NEMOCLAW                              │
│                                                             │
│  NVIDIA's version of OpenClaw                              │
│  Adds: security layer, permission controls, privacy router  │
│  Runs locally on NVIDIA hardware (RTX, DGX)                │
│  Targets enterprises that need OpenClaw but safely         │
│  Still no visual canvas. Still no workflow builder.        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      EMPYRALIST                             │
│                                                             │
│  Visual platform to BUILD and ORCHESTRATE agents           │
│  You SEE the workflow as a canvas                          │
│  Multiple agents talk to each other                        │
│  Designed for non-developers                               │
│  Local execution + cloud execution                         │
│  You build the agent team, not just one agent              │
└─────────────────────────────────────────────────────────────┘


The gap nobody has filled

          COMPLEXITY
               │
    Developers │  n8n ──────── (powerful, too technical)
               │
               │           ← YOU LIVE HERE →
               │  Empyralist (visual + agent-first + local)
               │
 Non-developers│  OpenClaw ─── (powerful, but just chat)
               │
               └────────────────────────────── POWER
                   Low                        High


The agentic AI market hit $7.8 billion and is projected to reach $52 billion by 2030. ￼ Nobody has a visual, non-developer, multi-agent orchestration platform with local execution. That is your hole.

How the execution layers work

┌─────────────────────────────────────────────────────────────┐
│                    LAYER 1: UI LAYER                        │
│                   (What user sees)                         │
│                                                             │
│   Browser/Desktop App                                       │
│   Next.js canvas                                           │
│   User describes workflow in plain language                │
│   Agent builds the canvas visually                         │
│   User presses Run                                         │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                  LAYER 2: DECISION LAYER                    │
│               (Where does this run?)                       │
│                                                             │
│   IF: workflow contains sensitive data                      │
│       → route to LOCAL execution engine                    │
│                                                             │
│   IF: workflow is generic (weather, public APIs etc)       │
│       → route to CLOUD execution engine                    │
│                                                             │
│   This router lives in your FastAPI server.py              │
└──────────────┬────────────────────────┬─────────────────────┘
               │                        │
               ▼                        ▼
┌──────────────────────┐    ┌───────────────────────────────┐
│   LAYER 3A: LOCAL    │    │     LAYER 3B: CLOUD           │
│                      │    │                               │
│  Runs on user's      │    │  Runs on your Hetzner VPS     │
│  machine             │    │  ~$20/month                   │
│                      │    │                               │
│  Ollama (local LLM)  │    │  GPT-4o / Claude API         │
│  Playwright          │    │  Shared execution pool        │
│  File system access  │    │  Multi-tenant                 │
│  Local APIs          │    │                               │
│                      │    │  Data leaves user machine     │
│  Data NEVER leaves   │    │  (non-sensitive only)         │
│  the building        │    │                               │
└──────────────────────┘    └───────────────────────────────┘
               │                        │
               └────────────┬───────────┘
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  LAYER 4: AGENT LAYER                       │
│              (Who actually does the work)                  │
│                                                             │
│   Orchestrator Agent  → reads the workflow, assigns tasks  │
│        │                                                   │
│        ├──► Agent A   → does task 1, writes report        │
│        ├──► Agent B   → does task 2, writes report        │
│        └──► Agent C   → does task 3, writes report        │
│                │                                           │
│                └──► Orchestrator reads reports             │
│                     decides next step                      │
│                     loops until done                       │
└─────────────────────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│                 LAYER 5: MEMORY LAYER                       │
│                                                             │
│   Working memory   → current task context                  │
│   Episodic memory  → what happened in past runs           │
│   Semantic memory  → facts about the user/business        │
│   Procedural       → how to do specific tasks             │
│                                                             │
│   Local: stored in SQLite on user machine                  │
│   Cloud: stored in your database on Hetzner               │
└─────────────────────────────────────────────────────────────┘


OpenClaw’s biggest weakness = your biggest opportunity
Because OpenClaw requires access to email accounts, calendars, messaging platforms, and system-level commands, it exposes users to numerous security vulnerabilities. A security audit identified 512 vulnerabilities, eight of which were classified as critical. ￼
OpenClaw’s design has drawn scrutiny from cybersecurity researchers because the software can access email accounts, calendars, messaging platforms, and other sensitive services — misconfigured or exposed instances present serious security and privacy risks. ￼
Empyralist’s answer to this:

OpenClaw problem          →    Empyralist solution
─────────────────────────────────────────────────
No permission controls    →    Per-agent permission scope
No visual audit trail     →    Canvas shows exactly what runs
Data goes everywhere      →    Local/cloud routing decision
Single user only          →    Team workspaces
No non-developer UI       →    Visual canvas, plain language
512 vulnerabilities       →    Security-first from day one


Who you are competing with directly

┌────────────────┬──────────────┬────────────┬──────────────┐
│ Product        │ Visual       │ Non-dev    │ Local exec   │
├────────────────┼──────────────┼────────────┼──────────────┤
│ n8n            │ ✅           │ ❌         │ ✅           │
│ OpenClaw       │ ❌           │ ✅         │ ✅           │
│ LangChain      │ ❌           │ ❌         │ ✅           │
│ CrewAI         │ ❌           │ ❌         │ ✅           │
│ Make.com       │ ✅           │ ✅         │ ❌           │
│ Zapier         │ ✅           │ ✅         │ ❌           │
│ EMPYRALIST     │ ✅           │ ✅         │ ✅           │
└────────────────┴──────────────┴────────────┴──────────────┘


Nobody checks all three boxes. You would be the first.

Pricing that makes sense given all this

FREE tier      → 1 workflow, cloud only, 100 executions/month
               → gets people in the door

PRO $49/month  → unlimited workflows, cloud execution
               → for individuals and freelancers

TEAM $199/month → multiple users, shared workspace
               → for small businesses

LOCAL $299/year → self-hosted, local execution agent
               → for privacy-first companies
               → YOUR biggest differentiator vs everyone


The local tier is what no competitor offers cleanly for non-developers. One user said running OpenClaw felt like Linux vs Windows 20 years ago — you are in control, you can hack it and make it yours instead of relying on some tech giant. ￼ That feeling, but with a clean visual UI and no 512 vulnerabilities, is exactly what Empyralist should be.​​​​​​​​​​​​​​​​