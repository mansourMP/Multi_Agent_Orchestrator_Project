{
  "name": "empyralis-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Persistent local gateway for Empyralis personal channels and local runtime control-plane connectivity.",
  "main": "dist/index.js",
  "scripts": {
    "build": "../bridge/node_modules/.bin/tsc -p tsconfig.json",
    "typecheck": "../bridge/node_modules/.bin/tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.1.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^7.0.0-rc.9",
    "pino": "^10.3.1",
    "telegram": "^2.0.0"
  }
}
