import WorkflowEditorClient from '../[id]/WorkflowEditorClient';

export default function NewWorkflowEditorPage() {
  return (
    <div style={{ minHeight: '100vh' }}>
      <WorkflowEditorClient />
    </div>
  );
}
